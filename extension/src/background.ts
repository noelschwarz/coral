/// <reference types="chrome" />

/**
 * Minimal MV3 service worker for Coral foundations.
 * Capture + daemon wiring arrives in week 1 (spec §3.1 Extension).
 */

chrome.runtime.onInstalled.addListener(() => {
  // Placeholder: extension installed or updated.
});

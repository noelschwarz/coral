/**
 * Capture helpers for cookies / storage — week 1 (spec §3.1 Extension).
 */

export async function captureActiveTabSession(): Promise<never> {
  throw new Error("captureActiveTabSession is not implemented yet (week 1).");
}
